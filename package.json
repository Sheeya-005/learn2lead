{
  "name": "raksha-women-safety",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install --legacy-peer-deps && npm --prefix client run build && npm --prefix server install --legacy-peer-deps",
    "start": "node server/server.js"
  }
}
